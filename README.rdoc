Before heroku
bundle exec rake assets:precompile

rake assets:precompile RAILS_ENV=production
RAILS_ENV=production bundle exec rake assets:precompile

New fields in the model=>do the permit

heroku db:reset to remove all stuff online.


delete your /public/assets directory it should fix it.

To debug rails routes puts #variable name 


Debug railstutorial
